import { Dimensions } from 'react-native';

export const screenWidth = Dimensions.get('window').width;
export const sliderWidth = screenWidth * 0.91;
export const itemHeight = 350; // Adjust this if your image height changes
